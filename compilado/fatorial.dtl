fun fatorialRecur {
  if(count1 > 1){
    x = x * count1;
    count1 = count1 - 1;

    fatorialRecur();
  }
}

fun fatorialIter {
  var count2;
  count2 = y - 1;

  while(count2 > 1){
    y = y * count2;
    count2 = count2 - 1;
  }
}

var x;
var y;

print("Entre com o x para o fatorial Recursivo:");
scan(x);
count1 = x - 1;

fatorialRecur();
print("Recursivo:", x);

print("Entre com o y para o fatorial Iterativo:");
scan(y);

fatorialIter();
print("Iterativo:", y);